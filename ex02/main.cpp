#include <iostream>
#include "Array.hpp"

#define MAX_VAL 750

int main(int, char**)
{
    // Array<int> numbers1; // Construction with no parameter:
	// Array<int> numbers2(MAX_VAL);

	// Array<int> numbers4(MAX_VAL);

	// Array<int> numbers3(numbers2);
	// numbers2 = numbers3;

    Array<int> numbers(MAX_VAL);
    int* mirror = new int[MAX_VAL];
    srand(time(NULL));
    for (int i = 0; i < MAX_VAL; i++)
    {
        const int value = rand();
        mirror[i] = value;
        numbers[i] = value;
    }
    //SCOPE
    {
        Array<int> tmp = numbers;
        Array<int> test(tmp);
    }

    for (int i = 0; i < MAX_VAL; i++)
    {
        if (mirror[i] != numbers[i])
        {
            std::cerr << "didn't save the same value!!" << std::endl;
            return 1;
        }
    }
    try
    {
        numbers[-2] = 0;
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }
    try
    {
        numbers[MAX_VAL] = 0;
    }
    catch(const std::exception& e)
    {
        std::cerr << e.what() << '\n';
    }

    for (int i = 0; i < MAX_VAL; i++)
    {
        numbers[i] = rand();
    }
   delete [] mirror;//
   //system("leaks iter");
   return 0;
}